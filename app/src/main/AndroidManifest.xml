<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pol.prashant.sample.physicsanimation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BackToMyPositionSpringAnimationActivity" android:label="@string/str_back_to_my_position"/>
        <activity android:name=".EnlargeAsGoDownSpringAnimation" android:label="@string/str_enlarge_as_go_down"/>
        <activity android:name=".SelectableImageDemo" android:label="@string/str_selectable_image_demo"/>
        <activity android:name=".FollowMeSpringAnimation" android:label="@string/str_follow_me_animation"/>
    </application>

</manifest>